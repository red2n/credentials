# NPM Configuration for API Service
# Fix deprecated package warnings and dependency resolution

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true

# Enable package overrides
fund=false
audit=false

# Prefer newer versions of packages
prefer-online=true
